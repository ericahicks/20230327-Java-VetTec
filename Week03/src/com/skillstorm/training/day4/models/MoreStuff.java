package com.skillstorm.training.day4.models;
// Situation 2 inside the same package
public class MoreStuff extends Stuff {

	// in stuff we can use it
		public void printX() {
			System.out.println(x);
		}
}
